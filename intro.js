 /* alert("Hello, World!");

function formulaX (n) {
	if (n === 1) return 1;
	return 1/n + formulaX(n-1);
}

alert (formulaX(1));
alert (formulaX(5));
alert (formulaX(10));  

function getFib(n) {
	if (n <= 1) return n;
	return getFib(n-1) + getFib(n-2);
}

alert(getFib(1));
alert(getFib(2));
alert(getFib(4));
alert(getFib(6));  */ 

//alert ( 2.toString(2) );


/* function Calculator() {
	
	read : function() {
		this.firstValue = +prompt ('Введите первое свойство');
		this.secondValue = +prompt ('Введите второе свойство');	
	}
	
	this.sum = function() {
		if ((!this.firstValue) || (!this.secondValue)) { alert('Некорректный ввод');
		} else { return this.firstValue + this.secondValue ;}
	}
	
	this.mul = function() {
		if ((!this.firstValue) || (!this.secondValue)) { alert('Некорректный ввод');
		} else { return this.firstValue * this.secondValue ;}
	}
	
}

var calculator = new Calculator();

calculator.read();
alert( "Сумма=" + calculator.sum() );
alert( "Произведение=" + calculator.mul() ); */

